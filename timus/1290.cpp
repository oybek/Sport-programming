
#include <cmath>
#include <cstdio>
#include <cctype>
#include <string>
#include <vector>
#include <cstdlib>
#include <iostream>
#include <algorithm>

void printer(vector<int> & a, vector<int> & b) {

	b.resize(0);
	b.push_back(a.size());

	for (auto e : a) {
		for () {
		}
	}
}

int main() {
	using namespace std;

	int size;
	vector<int> a, b;
	a.reserve(25001);
	b.reserve(25001);

	cin >> size;
	a.resize(size);
	for (auto & e: a) cin >> e;

	return 0;
}

